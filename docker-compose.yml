services:
  dashboard:
    build:
      context: .
    container_name: mobighor-dashboard
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
